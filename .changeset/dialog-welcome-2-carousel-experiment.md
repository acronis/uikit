---
'@acronis-platform/ui-react': minor
---

Add experimental `DialogWelcome2`, `DialogFooterCarousel2`, and `CarouselDialog2` — a parallel, from-scratch onboarding dialog + carousel footer built on Embla, alongside the existing (unmodified) implementation. Naming is temporary (the `2` suffix) pending a decision on what to keep/rename.
