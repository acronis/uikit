---
'@acronis-platform/ui-react': patch
---

Add a `Card` Storybook story showing content that overflows both axes, to
document the current clip-not-scroll behavior coming from `Card`'s root
`overflow-hidden`.
