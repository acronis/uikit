---
'@acronis-platform/tokens-pd': minor
---

Generate a full margin/padding/gap utility grammar directly from the existing `units.gap` primitive scale (0–96px) — `--ui-gap-*` custom properties, framework-agnostic `.ui-p-*`/`.ui-m-*`/`.ui-gap-*` classes (+ `.ui-mx-auto`) in `tokens-pd/css/default.css`, and `gap-*` keys in the Tailwind presets (`p-gap-8`, `gap-x-gap-16`, …).
