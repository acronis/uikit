---
'@acronis-platform/ui-react': minor
---

Add `CalendarPanel`: a bordered, elevated date-picker panel built on `react-day-picker`, with `single`, `multiple`, and `range` selection variants (the latter two adding a Cancel/Apply footer), themed by the new `--ui-calendar-*` token tier.
