---
'@acronis-platform/tokens-pd': minor
---

Add the remaining sizing utility classes from PLTFRM-94106: `.ui-w-*`/`.ui-h-*` fraction classes mirroring Tailwind's default fraction scale (halves through twelfths), a named `.ui-max-w-xs`...`.ui-max-w-7xl` scale, and position utilities — `.ui-static`/`.ui-relative`/`.ui-absolute`/`.ui-fixed`/`.ui-sticky` plus `.ui-top-*`/`.ui-right-*`/`.ui-bottom-*`/`.ui-left-*`/`.ui-start-*`/`.ui-end-*`/`.ui-inset-*`/`.ui-inset-x-*`/`.ui-inset-y-*` offsets (including a `-full` variant) generated from the same spacing scale.
