---
'@acronis-platform/tokens-pd': minor
---

Add `.ui-w-*`/`.ui-h-*`/`.ui-min-w-*`/`.ui-min-h-*`/`.ui-max-w-*`/`.ui-max-h-*` sizing utility classes, generated from the same spacing scale as `.ui-p-*`/`.ui-m-*`/`.ui-gap-*`, plus a static `.ui-max-w-full`, for framework-agnostic consumers who don't extend the Tailwind preset.
