{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://acronis.com/ui-spec/index.schema.json",
  "title": "Component spec — index.yaml",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "spec_version",
    "component",
    "name",
    "status",
    "category",
    "description"
  ],
  "properties": {
    "spec_version": { "type": "string" },
    "component": { "type": "string", "pattern": "^[A-Z][A-Za-z0-9]*$" },
    "name": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
    "sourceDir": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "ui-react component dir (under src/components/ui) this spec documents, when it differs from `name` — e.g. a component whose source is colocated inside another component's folder."
    },
    "status": { "enum": ["draft", "ready", "stable"] },
    "visibility": {
      "enum": ["public", "internal"],
      "description": "public (default, importable by devs) or internal (not exported from the package root — a primitive consumed only by other components)."
    },
    "deprecated": {
      "type": "object",
      "additionalProperties": false,
      "required": ["replacement"],
      "properties": {
        "since": { "type": "string" },
        "replacement": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "The `name` of the component spec that replaces this one."
        },
        "reason": { "type": "string" }
      }
    },
    "category": { "type": "string" },
    "description": { "type": "string" },
    "since": { "type": "string" },
    "dependencies": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "components": { "type": "array", "items": { "type": "string" } }
      }
    },
    "figma": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "node": { "type": "string" },
        "codeConnect": { "type": "string" }
      }
    }
  }
}
