spec_version: '1.0'
component: Separator
name: separator
status: ready
category: layout
description: >-
  A thin rule that visually and semantically divides content. Built on the Base
  UI Separator primitive (sets the `separator` role + aria-orientation). Ported
  from the legacy shadcn-uikit `Separator`; uses the shared divider token.
since: 0.15.0
figma:
  node: '788-15147'
  codeConnect: packages/ui-react/src/components/ui/separator/separator.figma.tsx
