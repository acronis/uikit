# PageContent — behavior

```gherkin
Scenario: Padded body region
  Given a PageContent inside AppShellChatContentBody
  Then the page body renders with the standard page padding and fills the area
```
