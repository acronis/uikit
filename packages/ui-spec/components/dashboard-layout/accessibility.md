# DashboardLayout — accessibility

DashboardLayout and DashboardGrid are presentational layout `<div>`s with no
semantics of their own; children follow source order. Give the dashboard region a
heading (e.g. a `PageHeader`) and the widgets their own accessible names.

## Contrast

No color of its own; widgets carry their own colors.
