spec_version: '1.0'
component: DialogWelcome2
name: dialog-welcome-2
status: draft
category: overlay
description: >-
  Experimental — an onboarding dialog with two layouts: `carousel` (a
  multi-slide feature tour driven by a real Embla carousel engine, with a
  `DialogFooterCarousel2` footer) and `single` (one image/title/description
  body with a primary action and a `Close` link). Parallel to a future
  onboarding-dialog primitive; not yet renamed/kept. Built from scratch —
  does not reuse any existing carousel implementation in this repo.
since: 0.58.0
dependencies:
  components:
    - Button
    - DialogFooterCarousel2
figma:
  node: '7162-26459'
  codeConnect: packages/ui-react/src/components/ui/dialog-welcome-2/dialog-welcome-2.figma.tsx
