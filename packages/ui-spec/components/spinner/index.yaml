spec_version: '1.0'
component: Spinner
name: spinner
status: stable
visibility: internal
deprecated:
  since: '0.58.0'
  replacement: loading
  reason: Superseded by Loading, which adds a label and container.
category: feedback
description: >-
  An indeterminate loading indicator — a spinning ring in four sizes. Ported from
  the legacy shadcn-uikit `Spinner`; a CSS spinner (no primitive), colored by the
  shared brand token via currentColor.
since: 0.15.0
