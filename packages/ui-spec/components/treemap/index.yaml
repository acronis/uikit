spec_version: '1.0'
component: Treemap
name: treemap
status: draft
category: data-display
description: >-
  A typed treemap composition over the shared `Chart` primitives. Tiles a flat
  set of leaves into nested rectangles sized by `dataKey` and colored per
  `nameKey`, inside a `ChartContainer`, with one- or two-line on-cell labels, a
  tooltip and an opt-in legend. No axes or grid — and no variant axes;
  `aspectRatio` is a plain prop.
  Themed through a custom cell renderer (recharts' default has no token hooks).
  Ported from the apps/demo `TreemapChartPlayground`.
since: 0.58.0
figma:
  node: 'https://www.figma.com/design/lrU3ydIyvPYQNE6ixdsKtJ/ui-react?node-id=8999-72036'
  codeConnect: packages/ui-react/src/components/ui/treemap/treemap.figma.tsx
