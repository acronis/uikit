spec_version: '1.0'
component: Treemap

# References only. Values live in @acronis-platform/tokens-pd. Treemap has no
# token tier of its own, and needs none: cell colours come from the shared
# `--ui-dataviz-*` palettes on the semantic tier, resolved by ChartContainer out
# of the `palette` prop. The tooltip chrome is inherited from the shared Chart
# primitives.
source: '@acronis-platform/tokens-pd'

tokens:
  - name: --ui-text-on-status-strong-neutral
    affects: cells
    description: >-
      On-cell label text on dark/saturated fills (diverging a3/b3, sequential
      stops 3–8, categorical, status) — white in both themes.
  - name: --ui-text-on-status-strong-primary
    affects: cells
    description: >-
      On-cell label text on sequential stops 7–8 whose fills invert across
      themes (dark in light, pale in dark). The token is near-white in light
      mode, near-black in dark — the exact inverse the mirrored fills need.
  - name: --ui-text-on-surface-primary
    affects: cells, tooltip-content
    description: >-
      On-cell label text on pale fills (diverging a2/b2/a1/b1, sequential
      stops 1–2) — dark in light, light in dark. Together with the on-strong
      tokens above, this implements the three-tone adaptive text color.
      Also used as tooltip value text (text-foreground).
  - name: --ui-background-surface-primary
    affects: tooltip-content
    description: Tooltip surface (bg-background).
  - name: --ui-text-on-surface-secondary
    affects: tooltip-content, legend-content
    description: >-
      Muted tooltip row label (text-muted-foreground), and a legend entry's
      custom `config` icon when one is given
      (`[&>svg]:text-muted-foreground`) — a legend entry's own label text
      inherits the surface's text color rather than this token.
  - name: --ui-border-on-surface-border
    affects: tooltip-content
    description: Tooltip border (border-border).
