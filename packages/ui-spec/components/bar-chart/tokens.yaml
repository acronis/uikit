spec_version: '1.0'
component: BarChart

# References only. Values live in @acronis-platform/tokens-pd. BarChart has no
# token tier of its own, and needs none: series colours come from the shared
# `--ui-dataviz-*` palettes on the semantic tier, resolved by ChartContainer out
# of the `palette` prop. The chrome (tooltip, legend, axes, grid) is inherited
# from the shared Chart primitives, which resolve the semantic tokens below.
source: '@acronis-platform/tokens-pd'

tokens:
  - name: --ui-background-surface-primary
    affects: tooltip-content
    description: Tooltip surface (bg-background).
  - name: --ui-text-on-surface-primary
    affects: tooltip-content
    description: Tooltip value text and axis titles (text-foreground / fill-foreground).
  - name: --ui-text-on-surface-secondary
    affects: x-axis
    description: >-
      Muted tooltip/legend label text and axis tick labels
      (text-muted-foreground / fill-muted-foreground).
  - name: --ui-text-on-surface-secondary
    affects: brush
    description: >-
      The range brush's border, its handle fill and its range captions — recharts
      derives all three from the single `stroke` prop. A mid-grey text token
      rather than the border one so the handles' hardcoded white grip lines stay
      visible; it is theme-stable, so they read in light and dark alike.
  - name: --ui-border-on-surface-border
    affects: grid
    description: >-
      Tooltip border, the recharts grid / cursor lines
      (border-border / stroke-border), and the dashed rule a `referenceArea`
      draws on its leading edge when `divider` is set.
  - name: --ui-background-surface-secondary
    affects: series
    description: >-
      Recharts muted fills — the tooltip cursor rectangle behind a hovered bar
      (fill-muted) — plus the `referenceArea` band and the default
      `backgroundFill` of a bar track.
  - name: --ui-background-surface-secondary
    affects: brush
    description: The range brush strip's background (recharts `fill`).
  - name: --ui-background-brand-primary
    affects: band
    description: >-
      Accent for the category ticks a `referenceArea` covers, so the highlighted
      range reads on the axis too (fill-primary). The ticks are the x-axis ones,
      which is the axis carrying the categories.
