spec_version: '1.0'
component: RadialBarChart

# References only. Values live in @acronis-platform/tokens-pd. RadialBarChart has
# no token tier of its own — arc colors are supplied by the caller via `config`
# (a dedicated `--ui-chart-*` data-viz palette is pending an upstream design
# pass). The chrome (tooltip, legend) and the background track are inherited from
# the shared Chart primitives.
source: '@acronis-platform/tokens-pd'

tokens:
  - name: --ui-background-surface-secondary
    affects: track
    description: >-
      The radial background track behind each arc (recharts
      radial-bar-background-sector → fill-muted), and the unreached segments of a
      segmented gauge, which play the same part.
  - name: --ui-text-on-surface-primary
    affects: center
    description: The center readout's headline value (fill-foreground).
  - name: --ui-text-on-surface-secondary
    affects: center
    description: The center readout's caption (fill-muted-foreground).
  - name: --ui-border-on-surface-border
    affects: grid
    description: The polar grid circles, when showPolarGrid is on (stroke-border).
  - name: --ui-text-on-status-strong-neutral
    affects: labels
    description: >-
      Arc data labels at an on-arc labelPosition (the insideStart default), which
      sit over a saturated arc fill and so take the on-series label token.
  - name: --ui-text-on-surface-primary
    affects: labels
    description: >-
      Arc data labels at an off-arc labelPosition (outside), which sit on the
      chart surface instead.
  - name: --ui-background-surface-primary
    affects: tooltip-content
    description: Tooltip surface (bg-background).
  - name: --ui-text-on-surface-primary
    affects: tooltip-content
    description: Tooltip value text (text-foreground).
  - name: --ui-text-on-surface-secondary
    affects: legend-content
    description: >-
      Muted tooltip/legend label text
      (text-muted-foreground / fill-muted-foreground).
  - name: --ui-border-on-surface-border
    affects: tooltip-content
    description: Tooltip border (border-border).
