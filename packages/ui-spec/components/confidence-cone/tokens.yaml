spec_version: '1.0'
component: ConfidenceCone

# References only. Values live in @acronis-platform/tokens-pd. ConfidenceCone has
# no token tier of its own — the metric colors are supplied by the caller via
# `config` (a dedicated `--ui-chart-*` data-viz palette is pending an upstream
# design pass). Each metric uses one color: its actual area, forecast line, and
# cone band all reuse its actual series' color (the cone/area fills at low
# opacity), and so do its dots and the styled forecast ticks. The chrome,
# forecast-region marker and thresholds resolve the semantic tokens below.
source: '@acronis-platform/tokens-pd'

tokens:
  - name: --ui-background-surface-secondary
    affects: region
    description: Subtle shaded background over the forecast region (fill-muted).
  - name: --ui-border-on-surface-border
    affects: divider
    description: The dashed vertical divider at the forecast hand-off (stroke-border).
  - name: --ui-background-surface-primary
    affects: tooltip-content
    description: Tooltip surface (bg-background).
  - name: --ui-background-surface-primary
    affects: forecast
    description: >-
      Fill of the hollow projected dots, so the dashed line does not show through
      their middle.
  - name: --ui-text-on-surface-secondary
    affects: threshold
    description: Stroke and caption of the dashed value-axis thresholds.
  - name: --ui-text-on-surface-primary
    affects: tooltip-content
    description: Tooltip value text and axis titles (text-foreground / fill-foreground).
  - name: --ui-text-on-surface-secondary
    affects: x-axis
    description: >-
      Muted tooltip/legend label text and axis tick labels
      (text-muted-foreground / fill-muted-foreground).
  - name: --ui-border-on-surface-border
    affects: grid
    description: Tooltip border and the recharts grid lines (border-border / stroke-border).
