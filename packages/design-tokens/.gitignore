# Local scratch space. Token refreshes are produced outside this monorepo, so any
# intermediate snapshot or script staged here is a working file — `tiers/` is the
# only committed source of truth.
.tmp/
