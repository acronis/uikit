# Local Figma token snapshots — reproducible pull inputs written by the
# sync flow (tools/figma-token-exporter → its receiver). Never committed; a
# contributor re-fetches them from Figma. The helper scripts under
# .tmp/scripts/ ARE committed and stay tracked (they're not matched here).
.tmp/figma-tokens/
