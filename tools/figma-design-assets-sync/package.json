{
  "name": "@acronis-platform/figma-design-assets-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Private build tool: sync SVG icons from a Figma frame into packages/design-assets and regenerate the pack manifest.",
  "type": "module",
  "scripts": {
    "build": "echo \"no build (runtime sync tool, run via tsx)\" && exit 0",
    "dev": "echo \"no dev server\" && exit 0",
    "clean": "echo \"nothing to clean\" && exit 0",
    "sync": "tsx src/index.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "echo \"no tests\" && exit 0",
    "test:watch": "echo \"no tests\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "figma",
    "design-assets",
    "icons",
    "svg",
    "acronis"
  ],
  "dependencies": {
    "chalk": "^6.0.0",
    "dotenv": "^17.4.2",
    "svgo": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.23.1",
    "typescript": "catalog:"
  }
}
