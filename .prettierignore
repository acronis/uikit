dist
node_modules
*.min.js
pnpm-lock.yaml

# Generated, committed token artifacts (built by tools/style-dictionary).
packages/tokens-pd/css
packages/tokens-pd/tailwind
packages/tokens-pd/dtcg

# Design-token source tiers are written by the external token-sync emitters, which
# own their house style. Reformatting them here would produce a spurious diff on
# every sync. Keep Prettier out of them.
packages/design-tokens/tiers

# Generated, committed icon-name map (scripts/generate-legacy-map.ts). Prettier
# rewraps its arrays into a different shape than the generator emits, which
# breaks the CI freshness check that compares the committed file to a fresh
# `generate` run. Keep Prettier out so the two stay byte-identical.
packages/icons-react/legacy-icon-map.json
