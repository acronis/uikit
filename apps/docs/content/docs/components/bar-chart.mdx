---
title: BarChart
description: A typed bar chart — grouped or stacked, vertical or horizontal.
---

import { DemoReact } from '@/components/DemoReact';
import { BarChartDemo } from '@/components/demos-react/bar-chart';

## Usage

```tsx
import { BarChart } from '@acronis-platform/ui-react';
import type { ChartConfig } from '@acronis-platform/ui-react';
```

`BarChart` is a typed recharts composition over the shared
[`Chart`](/components/chart) primitives. Give it `data`, a per-series `config`,
the series to plot (`dataKeys`), and the category key (`xKey`) — it renders a
themed recharts bar chart with tooltip, legend, axes, and grid, so you never
hand-compose recharts children.

Series colors are supplied by the caller through `config`: each entry maps a data
key to a `label` and a `color`. `BarChart` turns those into the `--color-<key>`
custom properties its bars fill from.

Use it to compare a quantity across categories (sales by month, usage by device),
including a few series per category — grouped or stacked. For a trend over a
continuous dimension prefer [`LineChart`](/components/line-chart) or
[`AreaChart`](/components/area-chart); for part-to-whole of a single total,
[`PieChart`](/components/pie-chart); for the distribution of a continuous
variable, [`Histogram`](/components/histogram) (which bins raw samples rather
than plotting pre-categorized rows). A single metric belongs in a
[`Tag`](/components/tag) or plain text, and row-level detail in a
[`Table`](/components/table) / [`DataTable`](/components/data-table).

Pair the chart with a text alternative (caption, summary, or adjacent data table)
and give it an accessible name — `aria-label` / `aria-labelledby` pass through to
the wrapper element. Keep either the legend or the tooltip visible so the series
aren't distinguished by color alone.

> **Design-pending v1**, ported from the demo `BarChartPlayground`. There is no
> chart token tier yet, so the example colors reference the shared semantic
> `--ui-*` brand/status tokens — a dedicated `--ui-chart-*` data-viz palette is
> pending an upstream design pass. `--ui-background-status-strong-*` is chromatic
> in every brand; `--ui-background-brand-secondary` is brand-dependent, so it is
> not color-stable across brands until the real palette lands.

## Examples

<DemoReact>
  <BarChartDemo />
</DemoReact>

A vertical, grouped bar chart with a themed tooltip and legend — the defaults:

```tsx
const data = [
  { month: 'Jan', desktop: 186, mobile: 80 },
  { month: 'Feb', desktop: 305, mobile: 200 },
  { month: 'Mar', desktop: 237, mobile: 120 },
];

const config = {
  desktop: { label: 'Desktop', color: 'var(--ui-background-brand-secondary)' },
  mobile: { label: 'Mobile', color: 'var(--ui-background-status-strong-danger)' },
} satisfies ChartConfig;

<BarChart
  config={config}
  data={data}
  dataKeys={['desktop', 'mobile']}
  xKey="month"
  className="h-[320px] w-[560px]"
/>;
```

Sum the series onto one bar per category with `layout="stacked"`. Only the last
segment's growing end is rounded:

```tsx
<BarChart config={config} data={data} dataKeys={['desktop', 'mobile']} xKey="month" layout="stacked" />
```

Lay the bars on their side with `orientation="horizontal"`. The category moves to
the y-axis and the grid flips with it:

```tsx
<BarChart config={config} data={data} dataKeys={['desktop', 'mobile']} xKey="month" orientation="horizontal" />
```

Toggle the chrome and corner rounding with `showGrid` / `showTooltip` /
`showLegend` / `barRadius`:

```tsx
<BarChart
  config={config}
  data={data}
  dataKeys={['desktop', 'mobile']}
  xKey="month"
  showLegend={false}
  barRadius={0}
/>
```

Overlay a dashed reference line on the value axis with `referenceLine` — either a
fixed target `value`, or a computed `average` (the mean of one series by key, or
of every plotted series when `true`). An optional `label` captions it, and the
line is orientation-aware. Pass an **array** to draw several at once:

```tsx
// Fixed target
<BarChart config={config} data={data} dataKeys={['desktop']} xKey="month"
  referenceLine={{ value: 250, label: 'Target' }} />

// Mean of the plotted series
<BarChart config={config} data={data} dataKeys={['desktop']} xKey="month"
  referenceLine={{ average: true, label: 'Average' }} />

// Several lines at once
<BarChart config={config} data={data} dataKeys={['desktop']} xKey="month"
  referenceLine={[{ value: 300, label: 'Target' }, { average: true, label: 'Average' }]} />
```

Add axis titles and a unit suffix with `xAxisLabel` / `yAxisLabel` / `yUnit`
(`xUnit` applies in `orientation="horizontal"`, where the x-axis is the numeric
one). The titles inherit the theme token, so they stay legible in both modes:

```tsx
<BarChart
  config={config}
  data={data}
  dataKeys={['desktop', 'mobile']}
  xKey="month"
  xAxisLabel="Month"
  yAxisLabel="Sessions"
  yUnit="k"
/>
```

Replace the tooltip with a configured `ChartTooltipContent` — imported from the
same library, so you still never compose recharts yourself. Its `formatter`
renders each row and `labelFormatter` the header:

```tsx
import { BarChart, ChartTooltipContent } from '@acronis-platform/ui-react';

<BarChart
  config={config}
  data={data}
  dataKeys={['desktop', 'mobile']}
  xKey="month"
  tooltipContent={
    <ChartTooltipContent
      labelFormatter={(label) => `${label} · fiscal Q3`}
      formatter={(value, name) => `${name}: ${value.toLocaleString()}k`}
    />
  }
/>;
```

## API Reference

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/bar-chart/bar-chart.tsx"
  name="BarChartProps"
/>

Each entry of `referenceLine` is a `BarChartReferenceLine`:

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/bar-chart/bar-chart.tsx"
  name="BarChartReferenceLine"
/>
