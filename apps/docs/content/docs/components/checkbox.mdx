---
title: Checkbox
description: A binary on/off selection, with optional label and description.
---

import { DemoReact } from "@/components/DemoReact";
import { CheckboxDemo } from "@/components/demos-react/checkbox";

## Usage

```tsx
import { Checkbox } from '@acronis-platform/ui-react';
```

`Checkbox` wraps Base UI's `Checkbox` primitive — keyboard interaction, focus
management, and ARIA come from Base UI. It supports `checked`, `unchecked`, and
`indeterminate` states, themed by the `--ui-checkbox-*` tokens.

## Examples


<DemoReact>
  <CheckboxDemo />
</DemoReact>

A bare checkbox (name it with `aria-label`):

```tsx
<Checkbox aria-label="Accept terms" />
```

The box is `align-middle`, so a bare checkbox stays vertically centered against
adjacent inline text (e.g. in a table cell) rather than sitting on the text
baseline.

With a `label` and optional `description` — the whole row becomes clickable:

```tsx
<Checkbox label="Email notifications" />

<Checkbox
  label="Email notifications"
  description="Send a summary every morning."
/>
```

Controlled:

```tsx
const [checked, setChecked] = React.useState(false);

<Checkbox label="Selected" checked={checked} onCheckedChange={setChecked} />
```

Indeterminate and disabled states:

```tsx
<Checkbox label="Mixed" indeterminate />
<Checkbox label="Locked policy" defaultChecked disabled />
```

`indeterminate` is set by the consumer and is independent of `checked` —
clicking does not clear it, and it wins over `checked` for the rendered glyph
and `aria-checked="mixed"`.

## API Reference

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/checkbox/checkbox.tsx"
  name="CheckboxProps"
/>
