---
title: ScatterChart
description: A typed scatter / bubble chart for correlation across two or three numeric fields.
---

import { DemoReact } from '@/components/DemoReact';
import { ScatterChartDemo } from '@/components/demos-react/scatter-chart';

## Usage

```tsx
import { ScatterChart } from '@acronis-platform/ui-react';
import type { ChartConfig } from '@acronis-platform/ui-react';
```

`ScatterChart` is a typed composition over the shared [`Chart`](/components/chart)
primitives. Give it a `series` list (each `{ key, data }` with its own points), a
per-series `config`, and the numeric axis fields (`xKey` / `yKey`) — it renders a
themed recharts scatter chart (tooltip, legend, axes, grid included), so you
don't hand-compose recharts children. Add a `zKey` to size points by a third
field (a bubble chart).

Each series carries its **own** point array (points aren't columns of a shared
row the way bar/line/area series are), so the API takes a `series` list rather
than `dataKeys`. Series colors are supplied by the caller through `config`, keyed
by each `series[].key`: `ScatterChart` turns those into the `--color-<key>`
custom properties its points fill from.

> **Design-pending v1**, ported from the demo `ScatterChartPlayground`. There is
> no chart token tier yet, so the example colors reference the shared brand/status
> tokens — a dedicated `--ui-chart-*` data-viz palette is pending an upstream
> design pass. `--ui-background-status-strong-*` is chromatic in every brand;
> `--ui-background-brand-secondary` is brand-dependent, so it is not color-stable
> across brands until the real palette lands.

## Examples

<DemoReact>
  <ScatterChartDemo />
</DemoReact>

Two groups plotted on shared x/y axes, with a themed tooltip and legend:

```tsx
const series = [
  { key: 'classA', data: [{ hours: 2, score: 55 }, { hours: 6, score: 78 }] },
  { key: 'classB', data: [{ hours: 1, score: 70 }, { hours: 9, score: 95 }] },
];

const config = {
  classA: { label: 'Class A', color: 'var(--ui-background-brand-secondary)' },
  classB: { label: 'Class B', color: 'var(--ui-background-status-strong-danger)' },
} satisfies ChartConfig;

<ScatterChart
  config={config}
  series={series}
  xKey="hours"
  yKey="score"
  className="h-[360px] w-[520px]"
/>;
```

Turn it into a bubble chart by mapping a third numeric field to point size with
`zKey`:

```tsx
<ScatterChart config={config} series={series} xKey="hours" yKey="score" zKey="weight" zRange={[60, 500]} />
```

Change the marker with `shape`, and toggle the chrome with `showGrid` /
`showTooltip` / `showLegend`:

```tsx
<ScatterChart config={config} series={series} xKey="hours" yKey="score" shape="triangle" showLegend={false} />
```

Add axis titles and unit suffixes with `xAxisLabel` / `yAxisLabel` / `xUnit` /
`yUnit` — both axes are numeric here, so both units apply. The titles inherit
the theme token:

```tsx
<ScatterChart
  config={config}
  series={series}
  xKey="hours"
  yKey="score"
  xAxisLabel="Hours studied"
  yAxisLabel="Score"
  yUnit="%"
/>
```

## Custom tooltip

Replace the tooltip with a configured `ChartTooltipContent` — imported from the
same library, so you never compose recharts yourself. Its `formatter` renders
each row and `labelFormatter` the header, giving you custom formatting, per-series
content, and extra fields:

```tsx
import { ScatterChart, ChartTooltipContent } from '@acronis-platform/ui-react';

<ScatterChart
  config={config}
  series={series}
  xKey="hours"
  yKey="score"
  tooltipContent={
    <ChartTooltipContent
      hideLabel
      formatter={(value, name) => `${name}: ${value.toLocaleString()}`}
    />
  }
/>;
```

Format the tick values with `xTickFormatter` / `yTickFormatter`, or hide an axis
with `showXAxis` / `showYAxis` — the shared axis knobs described under
[Formatting and hiding axes](/components/chart#formatting-and-hiding-axes). A
formatter can map numbers to labels, which the `unit` suffix can't:

```tsx
import { ScatterChart } from '@acronis-platform/ui-react';

<ScatterChart
  config={config}
  series={series}
  xKey="hours"
  yKey="score"
  yTickFormatter={(score) =>
    Number(score) >= 80 ? 'High' : Number(score) >= 60 ? 'Mid' : 'Low'
  }
/>;
```

## Animation

Charts render statically by default. Opt in to an entrance animation with
`animate`, and tune it with `animationDuration` / `animationBegin` /
`animationEasing`:

```tsx
<ScatterChart config={config} data={data} animate animationDuration={800} animationEasing="ease-out" />
```

`animate` honors `prefers-reduced-motion`: for a visitor who has asked their
system to reduce motion, the series render at their final geometry with no
animation (the same applies when rendering on the server).

## API Reference

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/scatter-chart/scatter-chart.tsx"
  name="ScatterChartProps"
/>
